[package]
name = "nexus-state-machine"
version = "0.1.0-alpha"
edition = "2021"

[dependencies]
nexus-core = { path = "../nexus-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-trait = "0.1"
thiserror = "2.0"

[dev-dependencies]
tokio = { version = "1", features = ["full", "test-util"] }
