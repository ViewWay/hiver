[package]
name = "hiver-state-machine"
version.workspace = true
edition.workspace = true

[dependencies]
hiver-core = { path = "../hiver-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-trait = "0.1"
thiserror = "2.0"

[dev-dependencies]
tokio = { version = "1", features = ["full", "test-util"] }
